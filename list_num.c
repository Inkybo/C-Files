#include <stdio.h>

int main()
{
	int start = 0;
	int end = 0;

	printf("enter a starting number: ");
	scanf("%d", &start);

	printf("enter a ending number: ");
	scanf("%d", &end);

	while(start <= end )
	}
		printf("%d \n", start);
		start = start + 1;
	}

	return 0;
}
