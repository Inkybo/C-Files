#include <stdio.h>

int main()
{
	char word[25];
	char test[20];

	strcpy(word, test); // word = test

						    // <0 if word is before test
	x = strcmp(word, test); //  0 if word is same as test
						    // >0 if word is after test
	if(strcmp(word, test))

	return 0;
}
